const name='Raji';
const Last_name='Sakthi';
const city='Namakkal';
const favorite_hobby='hearing songs';

console.log(`Hi, I'm ${name} ${Last_name} from ${city} and i love ${favorite_hobby}.`);

console.log(`${name.length} 
${Last_name.toUpperCase()} 
${city.slice(3)}`);